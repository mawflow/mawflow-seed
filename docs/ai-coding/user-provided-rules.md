# 用户提供的 AI 编码规则

本文件只记录用户、客户、项目负责人明确给出的规则。AI 不应把自己的推断写入本文件。

## 编码边界

- 可改范围：待补充。
- 慎改范围：待补充。
- 禁改范围：待补充。

## 代码风格偏好

- 命名：待补充。
- 注释：待补充。
- 错误处理：待补充。
- 日志：待补充。
- UI/交互：待补充。

## 技术和依赖约束

- 是否允许新增依赖：待补充。
- 是否允许调整框架配置：待补充。
- 是否允许数据库结构变更：待补充。
- 是否允许发布脚本变更：待补充。

## 交付和验证要求

- 最低测试要求：待补充。
- 发布前检查要求：待补充。
- 文档更新要求：待补充。
- AI 每次完成一段任务、任务包子任务或其它可独立验证的里程碑后，只要产生了代码、配置、文档或脚本改动，就必须完成必要验证、提交并推送当前分支，并在推送后运行仓库级 mirror 计划命令，按有效计划同步镜像；不要等用户再次要求“提交 push”。commit message 和提交内容说明必须使用中文，清楚记录变更范围、验证结果和遗留风险。只有用户明确禁止 git 写入、没有实际变更、存在无法安全暂存的无关脏改动，或认证、网络、分支保护、远端拒绝导致无法 push 时，才可跳过，并必须在最终说明中写清原因。
- 完成任务时如果除了本次任务变动还有其它文件变动，推送原则是：先只提交并推送本次任务改动；再判断剩余变动，若有 `code/**` 业务代码、组件运行配置或组件内文件变动，不纳入顺手推送；若剩余变动全部是 `code/**` 之外且不属于禁提交范围的文件，可补一个独立中文 commit message 后一起推送。

## 冲突和例外

如用户规则与 AI 分析规则冲突，在此记录最终裁定。

| 日期 | 冲突点 | 用户裁定 | 影响范围 |
|---|---|---|---|
|  |  |  |  |
