schema_version: 2
app_runtime:
  defaults:
    business_config_source: code
    ai_debug_source: maw
    conflict_policy: code_wins_and_update_maw
    default_profile: dev
    closeout_local_entry_required_when_configured: true
    local_environment_install_hint: "#安装开发环境"
  # 组件初始化不自动猜测运行端口或创建应用；需要时由项目按事实登记。
  apps: {}
