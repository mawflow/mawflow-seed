# ChatGPT 到 AI 的通用任务交接协议

本文件用于把已经讨论清楚的方案交给 Codex、Claude、Gemini、Cursor 或其它可执行 AI。它只规定交接内容，不替代目标项目自己的规则。

## 输出要求

默认输出一份可复制的 Markdown，不默认生成 zip。交接内容至少包含：

1. 一句话目标与业务结果。
2. 允许修改的范围、禁止修改的范围和敏感信息边界。
3. 已确认事实、仍需接收方核验的假设。
4. 可执行步骤、验证命令和验收标准。
5. 交付状态口径，例如“已修改”“已验证”“已上线”“真人已验收”。

建议格式：

```markdown
# 任务：<具体目标>

## 范围
- 允许修改：...
- 禁止修改：...

## 已知事实
- ...

## 实施与验收
1. ...

## 交付要求
- 说明改动、验证证据、未完成项与风险。
```

## 接收方 AI 规则

- 先读取目标项目 `AI_START_HERE.md` 与 `.maw/agent-entry.yaml`，再按目标项目规则执行。
- 任务较大时，是否建立任务包、计划文件或独立状态文件由目标项目协议决定；交接方不硬编码某一种 AI 的目录。
- 项目根路径使用仓库相对路径；不得写入真实密钥、token、客户隐私、生产连接串或本机私有路径。
- 不能把计划完成等同于实现完成，也不能把自动化测试等同于真人验收。
- 需要危险写操作、生产发布或外部授权时，服从目标项目的审批边界。

## 给用户的速记

```text
请按 CHATGPT_TO_AI.md，把当前方案整理成可交给执行型 AI 的 Markdown 任务；写清范围、禁止项、验收和证据，不要默认生成压缩包。
```

接收方收到后，可在目标项目对话中说：

```text
请读取 AI_START_HERE.md，并按下面的任务交接完整实施、验证和收口：
<粘贴 Markdown>
```
