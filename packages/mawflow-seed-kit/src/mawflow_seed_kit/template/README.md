# __PROJECT_NAME__

该项目由 MAWflow Seed Contract v2 创建。初始化只建立项目治理骨架，不预设 `server`、`client` 或其它技术组件。

运行 `mawflow project doctor` 检查项目协议，使用 `mawflow component init <key> --type <type>` 创建组件。CLI 用法见 `MAWFLOW_CLI.md`，与 AI 对话的项目指令见 `PROJECT_COMMANDS.md`。
