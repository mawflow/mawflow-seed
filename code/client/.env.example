# 客户端环境变量示例
# 应用环境；本地为 local，远程测试为 test，生产为 production。
APP_ENV=local
# 应用名称；初始化项目时替换 project_key。
APP_NAME={ project_key }-client
# 客户端调用的 API 地址；AI 调试可在 .maw/secrets.yaml 的 app_debug.apps.client.api_base_url 中显式镜像。
API_BASE_URL=http://localhost:8000/api
